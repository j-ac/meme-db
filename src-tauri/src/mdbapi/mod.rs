mod mdbapi {
    //Stubs go here

    // fn get_files(folder: String, a: usize, b: usize) -> Vec<FileDetails>
    // fn get_folders() -> Vec<FolderDetails>

    // fn get_tags() -> Vec<TagDetails> 
    // fn post_tag(name: String, parents: Optional<Vec<TagID>>) -> Optional<TagID>
    // fn delete_tag(tag: TagID) -> bool

    // fn patch_file_tags(file: FileID, tags_add: Vec<TagID>) -> bool //Success
    // fn delete_file_tags(file: FileID, tags_removed: Vec<TagID>) -> bool
    // fn purge_file_tags(file: FileID)
    // fn put_file_tags(file: FileID, tags: Vec<TagID>) -> bool

    // There will be more functions regarding callbacks, events, and IPC
}
